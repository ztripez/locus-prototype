name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    name: Rust hygiene and dogfood
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Test locked
        run: cargo test --workspace --locked

      - name: Build locked
        run: cargo build --workspace --locked

      - name: Install Locus CLI
        run: cargo install --path crates/locus-cli --locked

      - name: Dogfood changed-only strict gate
        env:
          LOCUS_BASELINE: origin/main
        run: scripts/check-changed-strict.sh .
